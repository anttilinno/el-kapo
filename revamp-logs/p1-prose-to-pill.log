Done. Changes:

**view.go**
- `BoardView`: added `HintLine`, `PowerPreview` fields.
- `powerText(rank)` helper mirrors engine `actionFor` (7/8→peek own, 9/10→peek other, J/Q→blind swap).
- `buildView`: sets `HintLine` per phase/turn (peek, turnstart, drawn, action, off-turn "X is thinking…"), `PowerPreview` during drawn phase. `Flash` field kept — now feeds toast.

**board_frag.html**
- Removed all instruction `<p>`s (peek/take/swap/action prose).
- You-pill under felt: `HintLine` + known-total chip w/ tooltip breakdown (hidden in Hard, matching old `.mytotal` gate).
- `seen` class on own known cards (`$.IsMe && .Show`) → 👁 badge.
- `PowerPreview` label on discard target.
- `.flash` div → `.toast`.

**base.html** (CSS only)
- Dropped `.mytotal`/`.flash`; added `.you-pill`, `.you-total`, `.power-preview`, `.card.seen::after` (👁), `.toast` (fixed, auto-fade).

skipped: toast client JS — server-rendered `.toast` fades via CSS, no JS system. Add when SSE toasts need queueing/dedup.
skipped: multiswap prose ("select 2+ same-rank") — Phase-1 removes prose; discoverability moves to wiggle affordance (Phase 1 state machine, base.html — not this task).

**Build not run** — `go build ./...` denied by harness permission prompt (non-interactive session). Approve it / run manually to verify. Go changes are self-contained: new fields + one pure helper using existing `game.Rank`/`game.Jack`/`game.Queen` consts.
