Build blocked — sandbox denies `go build` without interactive approval. Template blocks balance (81 if/range + 4 define = 85 end). Code self-reviewed. Run `go build ./...` to confirm compile.

Done. Changes:

**`pkg/server/table.go`**
- `Init(name, sid, hard)` — seats creator alone, opens Lobby. No up-front humans/bots, no auto-start.
- `Join` — appends a human seat (cap 4), no auto-start.
- `FreeHumanSeat` — now "lobby open && <4 seats".
- New `doAddBot` / `doSetDifficulty` / `doDeal` (all admin/seat-0), wired into `ApplyMove`. `doDeal` builds `match` then `startRound`.

**`pkg/server/view.go`**
- `SeatView` +`IsMe` +`Empty`; `BoardView` +`CanDeal`.
- Lobby build pads to 4 seat rows; `Scores` loop moved after lobby + nil-`match` guard (admin can End from lobby).

**`pkg/server/server.go`** — `handleCreate` takes only name+difficulty; dropped `strconv`.

**Templates**
- `lobby.html` — landing asks name only.
- `board_frag.html` — lobby branch = seats screen: big table code, copy-invite button, 4 seat rows (empty → `+ bot` for admin + waiting text), EASY/HARD segmented control with consequence line, DEAL disabled `<2` players + helper. Header/sidebar/log now non-lobby only.
- `base.html` — lobby CSS.

Skipped: per-empty-seat unique `+ bot` (all empty rows post the same append — fine at 4-seat cap). Difficulty on landing page dropped (set on seats screen per plan); add back a hidden field if you want create-time default. Engine `pkg/game` untouched. Tests set seats/match manually — unaffected.
